# Model Registry Location

Training writes the champion reference JSON here:

`contracts/model_registry/response_current.json`

Do not check in a dummy file. This directory exists so the runtime can create the reference in a stable package-relative location.
