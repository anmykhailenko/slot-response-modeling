# Response Modeling

Active runtime surface for Phase-1 observational response model training, response scoring, and response monitoring.

Run all commands from the `response_modeling/` directory so config, report, and script-relative paths resolve consistently.

Runtime roots:

- `configs/`
- `scripts/`
- `sql/`
- `src/`

Primary ODPS source:

- modeling dataset: `pai_rec_prod.alg_uplift_phase1_response_dataset_di`
